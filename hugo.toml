baseURL = 'https://whlapinel.github.io/tochtlis-courses/'
locale = 'en-us'
title = 'Math Through the Real World'
theme = 'PaperMod'

[markup]
  [markup.goldmark]
    [markup.goldmark.parser]
      [markup.goldmark.parser.attribute]
        block = true
        title = true
    [markup.goldmark.renderer]
      unsafe = true

sectionPagesMenu = 'main'

[[menu.main]]
identifier = "course"
name = "Course"
url = "/course/"
weight = 10

[params]
ShowBreadCrumbs = true
ShowPostNavLinks = true

[params.profileMode]
enabled = true
title = "Math Through the Real World"
subtitle = "A hands-on math course for a curious 13-year-old. Every session starts with something weird — then we figure out why."

[[params.profileMode.buttons]]
name = "Start Learning"
url = "/course/"
